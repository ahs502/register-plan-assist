{
  "name": "planassist-server",
  "version": "1.0.0",
  "private": true,
  "description": "Intelligent Flight Scheduler - Server",
  "main": "dist/index.js",
  "scripts": {
    "build": "gulp build",
    "start": "node .",
    "dev": "nodemon --watch src --watch index.ts --ext ts,js,json --verbose --exec npm run dev-start",
    "dev-start": "npm run build && npm run dev-run",
    "dev-run": "cross-env NODE_ENV=development node dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gulp": "gulp",
    "tsc": "tsc"
  },
  "author": "Mahan Air",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "mongodb": "^3.2.4"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/mongodb": "^3.1.26",
    "@types/node": "^11.13.6",
    "cross-env": "^5.2.0",
    "del": "^4.1.0",
    "gulp": "^4.0.1",
    "gulp-typescript": "^5.0.1",
    "nodemon": "^1.18.11",
    "typescript": "^3.4.4",
    "watch": "^1.0.2"
  }
}
